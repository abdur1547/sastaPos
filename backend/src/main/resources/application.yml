spring:
  datasource:
    url: ${JDBC_DATABASE_URL:jdbc:postgresql://localhost:5433/sastaPos}
    username: ${JDBC_DATABASE_USERNAME:postgres}
    password: ${JDBC_DATABASE_PASSWORD:<<YOUR_PASSWORD>>}
    hikari:
      connection-timeout: 30000
      maximum-pool-size: 10
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        jdbc:
          lob:
            non_contextual_creation: true
        id:
          new_generator_mappings: true
  docker:
    compose:
      # Postgres is started manually via the root-level docker-compose.yml (shared with PowerSync).
      enabled: false
error:
  handling:
    http-status-in-json-response: true
    exception-logging: NO_LOGGING
    full-stacktrace-http-statuses: 5xx
    log-levels:
      5xx: ERROR
    codes:
      StoreBusinessSettingUnique: STORE_BUSINESS_SETTING_UNIQUE
      ProductNameUnique: PRODUCT_NAME_UNIQUE
      ProductSkuUnique: PRODUCT_SKU_UNIQUE
      ProductBarcodeUnique: PRODUCT_BARCODE_UNIQUE
springdoc:
  pathsToMatch: /, /api/**
